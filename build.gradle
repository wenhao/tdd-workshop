configure(subprojects) { project ->
    apply plugin: 'idea'
    apply plugin: 'java'
    apply plugin: 'maven'


    group = "com.github.wenhao"
    version = '1.0.0'

    repositories {
        mavenCentral()
    }

    compileJava {
        sourceCompatibility = 1.6
        targetCompatibility = 1.6
    }

    dependencies {
        compile(
                "com.google.guava:guava:$guavaVersion",
                "com.google.inject:guice:$guiceVersion"
        )

        testCompile(
                "junit:junit:$junitVersion",
                "org.hamcrest:hamcrest-library:$hamcrestVersion",
                "org.mockito:mockito-all:$mockitoVersion"
        )

        task sourcesJar(type: Jar) {
            from sourceSets.main.allSource
            classifier = 'sources'
        }

        task javadocJar(type: Jar, dependsOn: javadoc){
            classifier = 'javadoc'
            from javadoc.destinationDir
        }

        artifacts {
            archives jar
            archives sourcesJar
            archives javadocJar
        }

        task wrapper(type: Wrapper) {
            description = "Generates gradlew[.bat] scripts"
            gradleVersion = "2.2"
        }
    }

}

project("01-guess-number") { project ->
    description = "01-guess-number"
}

project("02-fizz-buzz-whizz") { project ->
    description = "02-fizz-buzz-whizz"
}

project("03-parking-lot") { project ->
    description = "03-parking-lot"
}

project("04-taxi") { project ->
    description = "04-taxi"
}

project("05-pos") { project ->
    description = "05-pos"
}